// Importing the SectionWrapper component from the file located at "./SectionWrapper"
import SectionWrapper from "./SectionWrapper";

// Exporting the imported SectionWrapper component for use in other parts of the application
export { SectionWrapper }